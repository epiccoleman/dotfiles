# vim: set ft=bash

export PATH=${PATH}:~/bin/flutter/bin

export VAULT_ADDR="https://vault.pillarlabs.io"

alias emacs="emacs-25.3 -nw"
alias labs="cd ~/labs"
alias shoes="/Applications/Shoes.app/Contents/MacOS/shoes"
alias ftl="cd ~/Library/Application\ Support/FasterThanLight"
alias tf="terraform"

export ANDROID_SDK=$HOME/Library/Android/sdk
export PATH=$ANDROID_SDK/emulator:$ANDROID_SDK/tools:$PATH

export PATH="/usr/local/bin:${PATH}"
export PATH="$HOME/.cargo/bin:${PATH}"

# this allows emacs / react mode to see tern, beautifyjs, etc. I'm not sure if this should always be on the path, but it works.
export PATH=${PATH}:/Users/eric/.asdf/installs/nodejs/8.11.3/.npm/bin/
export HOMEBREW_GITHUB_API_TOKEN="fd8ccec7c69a554aa8a833958c3556afe4cac406"
