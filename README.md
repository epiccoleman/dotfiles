# Installation
1. Install git, and homeshick
2. `homeshick clone epiccoleman/dotfiles`
3. Run linux/init_linux.sh if on linux
4. Run first_time_setup.sh


You can run `osx/osx-settings.sh` if on a mac. Doesn't do much right now.
